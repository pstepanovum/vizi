// Native Firebase is configured through GoogleService-Info.plist (see app.json
// "googleServicesFile"). @react-native-firebase/app initializes automatically
// on app start, so no manual initializeApp call is needed.
//
// Import services from here as they come online, e.g.:
//   import auth from '@react-native-firebase/auth';
//   import analytics from '@react-native-firebase/analytics';
//   import firestore from '@react-native-firebase/firestore';

export {};
